version: "3"
services:
  app:
    container_name: "python3.6"
    build:
      context: .
      dockerfile: ./Dockerfile
    image: python_exec
    tty: true
    volumes:
      - $PWD/:/app
    working_dir: /app
    command: python3.6 /app/src/exec.py
